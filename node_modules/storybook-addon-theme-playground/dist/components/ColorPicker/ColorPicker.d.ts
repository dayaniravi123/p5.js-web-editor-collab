export interface ColorPickerProps {
    iconBefore?: HTMLElement;
    description?: string;
    label: string;
    onChange: (hex: string) => void;
    value: string;
}
declare const ColorPicker: ({ iconBefore, description, label, onChange, value, }: ColorPickerProps) => JSX.Element;
export default ColorPicker;
