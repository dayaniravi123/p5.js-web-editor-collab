export interface CounterProps {
    iconBefore?: HTMLElement;
    label: string;
    onChange: (val: number, suffix: string | undefined) => void;
    value: number;
    description?: string;
    min?: number;
    max?: number;
    steps?: number;
    suffix?: string | undefined;
}
declare const Counter: ({ label, description, min, max, steps, onChange, value, suffix, iconBefore, }: CounterProps) => JSX.Element;
export default Counter;
