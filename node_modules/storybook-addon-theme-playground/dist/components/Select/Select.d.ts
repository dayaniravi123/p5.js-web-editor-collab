declare type OptionValue = string | number;
declare type Option<T extends OptionValue> = {
    value: T;
    label?: string;
};
export interface SelectProps<T extends OptionValue> {
    iconBefore?: HTMLElement;
    label?: string;
    description?: string;
    name?: string;
    onChange: (val: T) => void;
    value: T;
    options: Option<T>[];
}
declare function Select<T extends OptionValue>(props: SelectProps<T>): JSX.Element;
export default Select;
