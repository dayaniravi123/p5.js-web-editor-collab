import React from 'react';
import StyledRadioGroup from './RadioGroup.style';
import RadioOption from '../RadioOption/RadioOption';
import Label from '../Label/Label';
var RadioGroup = function (_a) {
    var label = _a.label, description = _a.description, iconBefore = _a.iconBefore, name = _a.name, value = _a.value, onChange = _a.onChange, options = _a.options;
    return (React.createElement(StyledRadioGroup, null,
        React.createElement(Label, { iconBefore: iconBefore, label: label, description: description }),
        options &&
            options.map(function (item) { return (React.createElement(RadioOption, { key: item.value, label: item.label, value: item.value, name: name, onChange: function () { return onChange(item.value); }, isChecked: value === item.value })); })));
};
export default RadioGroup;
