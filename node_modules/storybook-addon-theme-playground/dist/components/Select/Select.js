import React from 'react';
import Label from '../Label/Label';
import StyledSelect from './Select.style';
function Select(props) {
    var options = props.options, iconBefore = props.iconBefore, value = props.value, label = props.label, description = props.description, name = props.name, onChange = props.onChange;
    function handleOnChange(e) {
        var selectedIndex = e.currentTarget.selectedIndex;
        var selectedOption = options[selectedIndex];
        onChange(selectedOption.value);
    }
    return (React.createElement(StyledSelect, { htmlFor: label },
        React.createElement(Label, { iconBefore: iconBefore, label: label, description: description }),
        React.createElement("select", { value: value, onChange: handleOnChange, name: name }, options &&
            options.length > 0 &&
            options.map(function (option) { return (React.createElement("option", { value: option.value, key: option.value }, option.label || option.value)); })),
        React.createElement("div", { className: "select__icon" })));
}
export default Select;
