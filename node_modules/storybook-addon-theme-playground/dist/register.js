/* eslint-disable react/display-name */
import React from 'react';
import addons from '@storybook/addons';
import { AddonPanel } from '@storybook/components';
import Panel from './components/Panel/Panel';
import SettingsProvider from './contexts/SettingsProvider';
addons.register('storybook-addon-theme-playground', function (api) {
    addons.addPanel('storybook-addon-theme-playground/panel', {
        title: 'Theme Playground',
        render: function (_a) {
            var active = _a.active, key = _a.key;
            return (React.createElement(AddonPanel, { key: key, active: active },
                React.createElement(SettingsProvider, { api: api },
                    React.createElement(Panel, null))));
        },
    });
});
